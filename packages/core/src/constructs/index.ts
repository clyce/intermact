/**
 * Math construct library (design.md §7.4). Axis-bound graphs, planes, number
 * lines, matrices/tables, and braces. Axes (`getAxes`/`axesObject`),
 * `functionGraph`, and `decimalNumber` live in `layout/` and `text/` and are
 * exported from the package root alongside these.
 */
export * from "./shared";
export * from "./graphs";
export * from "./number-line";
export * from "./planes";
export * from "./matrix";
export * from "./table";
export * from "./brace";
